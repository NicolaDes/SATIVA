1) Aggiungi la sussunzione iniziale dove elimini le clausole contenute in altre (per ogni clausola se è minore elimini quella più grande)

2) Quando aggiungo una clausola controlla se è già true o se esiste il suo negato, se così allora non aggiungere tale clausola:
for (i = j = 0, p = lit_Undef; i < ps.size(); i++)
        if (value(ps[i]) == l_True || ps[i] == ~p)
            return true;
        else if (value(ps[i]) != l_False && ps[i] != p)
            ps[j++] = p = ps[i];

3) Prova ad implementare la versione di minisat come propagaione e guarda i tempi!! se va meglio capisci il perchè

4) Fai un metodo per rimuovere le clausole soddisfatte. Chiamalo ogni volta che sei al livello 0
